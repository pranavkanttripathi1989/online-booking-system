**Prompt: STRICT Frontend QA, Fix, Re-test & Optimization clinician dashboard **

You are required to perform a **complete, deterministic frontend QA and improvement cycle** for the **[MODULE_NAME]** module.

Follow every step exactly. Do not skip, reorder, or approximate any instruction.

---

## CONFIGURATION (EDIT ONLY THIS SECTION)

Replace the placeholders below before execution:

* RESULTS_FILE: `test-results-page-test-results.md`
* TEST_PLAN_FILE: `test-results-page-test-plan.md`
* SUGGESTION_FILE: `test-results-page-test-suggestion.md`
* UPDATED_TEST_PLAN_FILE: `test-results-page-test-plan.md`

---

## STEP 1: Parse Input File

* Read **RESULTS_FILE**
* Extract **all reported issues**
* Create a structured list of issues before proceeding

---

## STEP 2: Analyze and Fix Issues (Frontend Only)

For **each issue**, you MUST:

1. Identify root cause strictly from frontend perspective:

   * UI rendering issue
   * State management bug
   * API handling / mock issue
   * Validation issue
   * UX flaw
   * Edge-case handling gap

2. Apply a **production-ready fix**

3. Document in this exact format:

```
Issue ID:
Issue Description:
Root Cause:
Fix Implemented:
Code-Level Explanation:
Impacted Files:
```

Rules:

* Do not combine issues
* Do not skip any issue

---

## STEP 3: Re-test Using Test Plan

* Load **TEST_PLAN_FILE**
* Execute **100% of test cases**
* No skipping allowed

For each test case, validate:

* Form input + validation
* UI states (loading, success, error)
* API OR mock behavior
* Error handling

---

## STEP 4: Record Test Results

Update **RESULTS_FILE**

For **each test case**, use EXACT structure:

```
Test Case ID:
Title:

Input:
<user actions / form data>

Expected Output:
<expected UI behavior>

Actual Output:
<actual UI behavior>

Status:
PASS / FAIL

Observations:
<detailed notes: UI bugs, delays, inconsistencies>
```

Rules:

* No missing test cases
* No empty fields
* Must reflect latest behavior after fixes

---

## STEP 5: Implement Suggestions

* Read **SUGGESTION_FILE**
* Apply **all frontend-related suggestions**
* Validate each implementation via testing

---

## STEP 6: Update Suggestions File

Modify **SUGGESTION_FILE**

For each suggestion:

```
Suggestion:
Status: COMPLETED / PENDING
Notes:
```

Additionally:

* Add new suggestions discovered during testing
* Focus on:

  * UX
  * Performance
  * Accessibility
  * Responsiveness

---

## STEP 7: Update Test Plan

Modify **UPDATED_TEST_PLAN_FILE**

You MUST:

* Add new test cases for:

  * Edge cases
  * Validation failures
  * Loading states
  * Error states
  * Empty states
* Improve clarity of existing test cases

---

## STEP 8: Implement Mock Data Layer (MANDATORY)

Frontend MUST support mock mode.

Requirements:

* Create mock handling for:

  * Fetch
  * Create / Update / Delete
  * Error scenarios

* Add toggle mechanism:

  * Environment variable OR config flag

Example:

```
USE_MOCK_API=true
```

* Ensure app runs **without backend**

---

## STEP 9: Final Validation

You MUST confirm:

* All issues are fixed
* All test cases executed
* All failing tests are now passing
* Mock mode works correctly
* No UI inconsistencies remain

---

## FINAL OUTPUT (MANDATORY)

Provide ALL of the following:

1. Updated **RESULTS_FILE**
2. Updated **SUGGESTION_FILE**
3. Updated **UPDATED_TEST_PLAN_FILE**
4. Fix Summary:

```
Total Issues:
Fixed Issues:
New Issues Found:
Test Cases Passed:
Test Cases Failed:
```

---

## HARD RULES

* Do NOT skip any step
* Do NOT assume backend fixes
* Do NOT leave placeholders
* Do NOT produce partial output
* Ensure production-level quality

---

Execute with strict accuracy and completeness.
