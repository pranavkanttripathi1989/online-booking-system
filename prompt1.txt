# Prompt: STRICT Frontend QA, Fix, Re-test & Optimization (Real API Integration)

You are required to perform a **complete, deterministic frontend QA and improvement cycle** for the **[MODULE_NAME]** module, integrated against the **real backend API** (no mocks).

Follow every step exactly. Do not skip, reorder, or approximate any instruction.

---

## CONFIGURATION (EDIT ONLY THIS SECTION)

Replace the placeholders below before execution:

* RESULTS_FILE: `test-results-page-test-results.md`
* TEST_PLAN_FILE: `test-results-page-test-plan.md`
* SUGGESTION_FILE: `test-results-page-test-suggestion.md`
* UPDATED_TEST_PLAN_FILE: `test-results-page-test-plan.md`
* API_BASE_URL: `<insert real backend base URL, e.g. https://api.example.com/v1>`
* API_AUTH_METHOD: `<e.g. Bearer token / cookie session / API key header>`
* API_ENV: `<staging / production / local backend>`

---

## STEP 1: Parse Input File

* Read **RESULTS_FILE**
* Extract **all reported issues**
* Create a structured list of issues before proceeding

---

## STEP 2: Analyze and Fix Issues (Frontend Only, Real API Behavior)

For **each issue**, you MUST:

1. Identify root cause strictly from a frontend perspective, informed by **actual API responses** (status codes, payload shape, latency, error format) rather than assumed/mocked behavior:

   * UI rendering issue
   * State management bug
   * API request/response handling (auth headers, payload shape, status codes, timeouts, retries)
   * Validation issue (client-side + server-side error surfacing)
   * UX flaw
   * Edge-case handling gap (network failure, slow response, partial data, 4xx/5xx)

2. Apply a **production-ready fix**, verified against the live API_BASE_URL

3. Document in this exact format:

```
Issue ID:
Issue Description:
Root Cause:
Fix Implemented:
Code-Level Explanation:
Impacted Files:
API Endpoint(s) Involved:
Verified Against Real API: YES/NO
```

Rules:

* Do not combine issues
* Do not skip any issue
* Do not assume backend behavior — confirm it by making the real call

---

## STEP 3: Re-test Using Test Plan (Against Real API)

* Load **TEST_PLAN_FILE**
* Execute **100% of test cases** against the real API_BASE_URL (no stubbing)
* No skipping allowed

For each test case, validate:

* Form input + validation (client-side and server-validation-error rendering)
* UI states (loading, success, error) driven by actual request lifecycle
* Real API request/response cycle (headers, auth, payload, status codes)
* Error handling for real failure modes: 4xx, 5xx, timeouts, network drops, malformed responses

---

## STEP 4: Record Test Results

Update **RESULTS_FILE**

For **each test case**, use EXACT structure:

```
Test Case ID:
Title:

Input:
<user actions / form data>

API Call:
<endpoint, method, key request params>

Expected Output:
<expected UI behavior>

Actual Output:
<actual UI behavior, based on real API response>

Status:
PASS / FAIL

Observations:
<detailed notes: UI bugs, delays, inconsistencies, latency, unexpected payloads>
```

Rules:

* No missing test cases
* No empty fields
* Must reflect latest behavior after fixes, observed against the real API

---

## STEP 5: Implement Suggestions

* Read **SUGGESTION_FILE**
* Apply **all frontend-related suggestions**
* Validate each implementation via testing against the real API

---

## STEP 6: Update Suggestions File

Modify **SUGGESTION_FILE**

For each suggestion:

```
Suggestion:
Status: COMPLETED / PENDING
Notes:
```

Additionally:

* Add new suggestions discovered during testing
* Focus on:

  * UX
  * Performance (real network latency, request waterfalls, caching)
  * Accessibility
  * Responsiveness
  * API resiliency (retry/backoff, error messaging, stale-data handling)

---

## STEP 7: Update Test Plan

Modify **UPDATED_TEST_PLAN_FILE**

You MUST:

* Add new test cases for:

  * Edge cases
  * Validation failures (client + server-returned validation errors)
  * Loading states (including slow/real-latency conditions)
  * Error states (4xx, 5xx, timeout, network offline)
  * Empty states (real API returning no data)
  * Auth expiry / token refresh mid-session
* Improve clarity of existing test cases

---

## STEP 8: Real API Integration Layer (MANDATORY)

Frontend MUST be fully wired to the real backend — no mock/stub layer in the shipped build.

Requirements:

* Implement a single API client/service layer covering:

  * Fetch (GET)
  * Create / Update / Delete (POST/PUT/PATCH/DELETE)
  * Centralized error handling (network errors, non-2xx responses, timeouts)

* Configuration:

  * Base URL, auth method, and environment driven by config/env vars, e.g.:

```
API_BASE_URL=<real backend URL>
API_AUTH_METHOD=bearer
API_ENV=staging
```

* Include:

  * Request/response interceptors for auth token attachment and refresh
  * Consistent error normalization surfaced to the UI layer
  * Timeout and retry policy for transient failures
  * Loading/success/error state wiring tied to actual request lifecycle

* Confirm the app runs correctly end-to-end **against the live/staging backend**, with no residual mock/stub code paths left active by default

---

## STEP 9: Final Validation

You MUST confirm:

* All issues are fixed and verified against the real API
* All test cases executed against the real API
* All failing tests are now passing
* Real API integration works correctly across success, error, and edge-case scenarios
* No UI inconsistencies remain

---

## FINAL OUTPUT (MANDATORY)

Provide ALL of the following:

1. Updated **RESULTS_FILE**
2. Updated **SUGGESTION_FILE**
3. Updated **UPDATED_TEST_PLAN_FILE**
4. Fix Summary:

```
Total Issues:
Fixed Issues:
New Issues Found:
Test Cases Passed:
Test Cases Failed:
API Endpoints Verified:
```

---

## HARD RULES

* Do NOT skip any step
* Do NOT use mocked/stubbed API responses in the final validation — real API only
* Do NOT assume backend behavior without confirming via a real call
* Do NOT leave placeholders
* Do NOT produce partial output
* Ensure production-level quality

---

Execute with strict accuracy and completeness.