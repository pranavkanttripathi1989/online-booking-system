# Context — MediBook/HealthSync

Index of everything captured during backend-architecture planning. Read `frontend-contract-analysis.md` first if you're new to this — everything else is derived from it.

## Files in this folder

| File | What it is |
|---|---|
| `frontend-contract-analysis.md` | Ground truth: every route, every GraphQL operation the frontend expects (by domain), auth flow, the fact the app runs 100% on mock data today, real-time/upload/payment integration points, role enforcement gaps. |
| `backend-implementation-plan.md` | 17-phase step-by-step backend build plan (Node + NestJS + Apollo GraphQL + Prisma + PostgreSQL + Redis), grounded in the contract analysis. Includes India-specific decisions (Razorpay, MSG91/Gupshup OTP, AWS SES `ap-south-1`, GST invoicing, INR), Phase 3.5 (SaaS organization onboarding), plan-based entitlements, and organization branding/white-labeling. |
| `frontend-suggestions-implementation-log.md` | Tracks the wave of `test-suggestion/` fixes being implemented across 5 parallel background agents (🔴/🟡 priority items only, 🟢 deferred). |
| `test-cases-writing-log.md` | Tracks the 4 parallel background agents writing the formal test-case suite at `/test-cases` (14 feature domains × Unit/Backend-API/Functional-E2E/Frontend sections), written before backend development starts. |
| `frontend-hard-rules.md` | Mandatory frontend engineering standards (responsiveness, form validation, accessibility, error/loading states, testing, file structure, mock-vs-real-data hygiene) — each rule grounded in an actual audit of `frontend/src`, not generic boilerplate. Includes a literal Definition-of-Done checklist. Read before any further frontend work. |
| `backend-hard-rules.md` | Mandatory backend engineering standards for Phase 4 onward — multi-tenant `client_org_id` scoping, global-guard auth (corrected mid-Phase-4, see below), DTO validation, GraphQL error-formatting hygiene, Prisma transaction/migration discipline, secrets hygiene, real test coverage per phase, timing-safe auth patterns, GraphQL contract fidelity. Every rule grounded in an actual finding from real code or a real bug hit live. Includes a Definition-of-Done checklist. |
| `test-results-backend-implementation-plan.md` | Test Results (lab reports) — built, migrated, and live-verified, plus a full strict QA cycle re-run against the real API (36 cases, 0 failures). Built from scratch (zero pre-existing GraphQL contract). Documents the real design call: `patient` is free text today (no patient picker in the UI), so `patient_id` is an optional, nullable, forward-compatible FK rather than a hard requirement. Triggered by a request to run a strict real-API QA cycle that couldn't run against a nonexistent backend — see `test-plan/test-result/test-suggestion` `test-results-page-*` (v3.0) for the full cycle. |
| `phase4-5-increment3-implementation-plan.md` | Languages, EmailTemplates, Clinicians, and Services (Products core) — built, migrated, and live-verified (curl + real browser). Picked after auditing every candidate page's real GraphQL contract; documents why Communications/Policies/the full Products domain were excluded. Found and fixed a real bug (`EmailTemplate.type` never mapped from the `template_type` column). Confirms the `npm run build`-vs-`nest start --watch` `dist/` conflict is a recurring, avoidable issue — don't run both against the same container. See `test-plan/test-result/test-suggestion` `phase4-5-increment3-*` for full detail. |
| `phase4-catalog-modules-implementation-plan.md` | Phase 4 increments 1 (Clinics/Rooms/Clinician-Room-Type lookups) and 2 (Organizations admin CRUD) — built, migrated, and live-verified against the running Docker stack, including a real Playwright-MCP browser pass (not just `curl`) that found and fixed a second real bug: `/admin/room-types`/`/admin/clinician-types` writes were 100% broken (contract mismatch invisible to the earlier `curl`-only verification). See `test-plan/test-result/test-suggestion` `phase4-backend-integration-*` files for the full live-testing detail. Increment 1 also found and fixed a global-guard-ordering bug (corrected `backend-hard-rules.md` Rule 2 in place) and a `manager/clinics/index.jsx` zero-Apollo-wiring gap; `manager/rooms/index.jsx`'s incompatible GraphQL contract is still an open decision, not resolved. Increment 2 rebuilt `ClientOrganizations`' address as the proper India structured shape (state+pincode) and updated `admin/Organizations.jsx` to match. |
| `phase1-frontend-missing-features-implementation-plan.md` | Tracks frontend implementation of the Semble gap-analysis roadmap (Phases 1-6) against `frontend-hard-rules.md`. Status as of last update: 15 net-new features built across Phases 1-4 and 6 (Phase 6 adds duplicate-patient merging, Waiting Room/Journey tracking, and a patient communication log — all traced to specific findings from the live Semble docs cross-check), test infra stood up from nothing, one pre-existing responsive bug found and fixed. **Needs a live-browser verification pass** — every feature past the first (Custom Roles) is syntax-verified only, not yet run in a browser. Also has an honest breakdown of what was deliberately *not* built and why (large-file financial restructuring deferred for risk; payment terminals/ICD-10/webhook-delivery/payment-on-account/clinical-report-governance correctly out of scope or sequenced later). |
| `backend-api-requirements-master-plan.md` | Exhaustive, no-file-skipped audit of all 75 page files + 55 component files (4 parallel Explore agents), synthesized into decisions: the two competing GraphQL naming dialects (canonical snake_case vs. a "patient self-serve" camelCase surface), the three competing mutation-response wrapper conventions, entity-shape conflicts requiring a pick (Room, Product/Service, Availability, User), a list of orphaned components with real but unconsumed contracts, and a phase-grouped requirements breakdown (P5-P11) for every domain still lacking a backend. Confirms most remaining domains (Appointments, Reviews, Messages, Notifications, Availability/Blocks, Users/RBAC) already have fully-relational Prisma models scaffolded with zero resolvers — this is resolver-writing against an already-correct schema, not schema design from scratch. Feeds directly into `next-10-features-implementation-plan.md`. |
| `next-10-features-implementation-plan.md` | Detailed implementation plan (with hard rules) for the 10 highest-priority backend domains identified by the master plan, in dependency order: Appointments CRUD → real-time subscriptions → Available Slots → Clinician Availability/Lunch Breaks → Spacer/Room Blocks → User Management/RBAC → Staff → Notifications → Reviews → Messages. |

## Other top-level folders this project now has

- **`/requirements`** — business/product requirements docs: `organization-branding-and-management-requirements.md` (competitive-benchmarked branding/plan-tier recommendation), `security-requirements.md` (OWASP-grounded checklist every backend phase must satisfy — row-level tenant isolation is the single highest-risk gap identified so far, since the frontend enforces none of it), `semble-competitive-gap-analysis-requirements.md` (custom Roles/Access-Groups RBAC requirement + a full competitive gap analysis against Semble, phased by business priority — headline finding: MediBook is booking/scheduling-only today, Semble is a full clinical practice-management platform with consultations/diagnoses/prescriptions/labs that MediBook has none of).
- **`/test-cases`** — the formal pre-backend test-case suite (see `test-cases/README.md`), distinct from the pre-existing `/test-plan`, `/test-result`, `/test-suggestion` (manual QA history) — this new suite is derived from and cites that QA history, plus `frontend-contract-analysis.md` and `schema.prisma`, as its grounding. 15 domains now (added `15-tasks` — a domain with no `schema.prisma` model yet); `05-patients`/`12-admin-rbac`/`03-appointments-booking` were extended with cases for this session's Semble-gap-analysis mockups (patient merging, Waiting Room/Journey, communication log, `client_org_id`-scoped custom roles).

## Decisions made so far

- **Stack:** Node.js + NestJS + Apollo GraphQL (code-first) + Prisma + PostgreSQL + Redis (BullMQ + Pub/Sub), replacing the empty/unused Laravel scaffold in `backend/`.
- **Market:** India — Razorpay (patient payments), Stripe kept only for tenant SaaS-subscription billing, MSG91/Gupshup (OTP SMS), AWS SES `ap-south-1` (email), AWS `ap-south-1` hosting, GST fields on `PaymentTransactions`, INR stored as paise, Indian address format (state + PIN code).
- **`schema.prisma` extended** (root of repo, not yet moved into `backend/`): added `Reviews`, `MessageThreads`/`MessageParticipants`/`Messages` (previously undefined domains), GST + Razorpay fields on `PaymentTransactions`, all money fields switched `Float→Int` (paise), `OnboardingStatus` enum + `owner_user_id`/`onboarding_status`/`onboarding_step`/`trial_ends_at`/`onboarded_at` on `ClientOrganizations` for self-serve SaaS signup, extended `TemplateType` for the email service. Validated with `prisma validate` — schema is syntactically and relationally correct.
- **Organization onboarding wizard built** — `frontend/src/pages/onboarding/index.jsx` (4-step: org+owner → plan → first clinic → done), route `/get-started`, linked from the landing nav ("For Clinics"). Backed by real mock-store mutations (`startOrganizationOnboarding`, `selectOnboardingPlan`, `addOnboardingFirstClinic`, `completeOrganizationOnboarding` in `frontend/src/mocks/store.js`), not simulated. Verified working end-to-end via Playwright.
- **Mock data added:** `SUBSCRIPTION_PLANS` (starter/pro/enterprise, real INR pricing) in `frontend/src/mocks/data/seed.js`, wired into the shared store.

## Open / not yet built

- Backend project itself — `backend/` still holds the old empty Laravel scaffold; Phase 1 (NestJS scaffold + `docker-compose.yml` migration to Postgres) hasn't started.
- Organization branding UI (logo upload, color pickers) — recommendation documented, not built.
- Plan entitlement enforcement — recommendation documented, not built (no guard exists yet since there's no backend).
- Reviews/Messages GraphQL schema needs a short design pass with whoever owns frontend before Phase 11 (mock shapes were reverse-engineered, not confirmed as final).
- `test-suggestion/` implementation wave — in progress via 5 background agents; results get appended to `frontend-suggestions-implementation-log.md` as each completes. 🟢 Low priority items across all 47 files are explicitly deferred, not done.
